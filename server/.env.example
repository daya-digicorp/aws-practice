# Local laptop: leave SECRET_NAME unset and fill the values below.
# EC2: do not use this file. Set SECRET_NAME=todo-app/config and use an instance role.

PORT=4000
AWS_REGION=us-east-1
DYNAMODB_TABLE=todo
CORS_ORIGIN=http://localhost:5173
# SECRET_NAME=todo-app/config

# Local only (EC2 should use an IAM instance role instead)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
